/*
 * ESP-IDF port placeholder for the NXP embedded example boot hook.
 *
 * The SE050 transport is initialized by se05x_bus and i2c_a7_se05x_bus.c in
 * this project, so the upstream example header does not need to provide board
 * startup services here.
 */
#pragma once

